[package]
name = "s3-grabber"
version = "0.1.0"
edition = "2021"

[dependencies]
ratatui = "0.30.0"
crossterm = "0.29.0"
tokio = { version = "1", features = ["rt", "time"] }
aws-config = "1"
aws-sdk-s3 = "1"
aws-types = "1"
